/* =====================================
   RPL CYBER SECURITY V2
===================================== */

"use strict";

/* ==========================
   Translations
========================== */

const translations = {

en:{

nav_home:"Home",
nav_about:"About",
nav_skills:"Skills",
nav_contact:"Contact",

loader_text:"Initializing Secure Environment...",

hero_terminal:"root@rpl:~$ system_online",
hero_desc:"Building secure digital solutions through Cyber Security, Programming, Security Research and Ethical Hacking.",

btn_skills:"Explore Skills",
btn_contact:"Contact Me",

about_title:"About Me",
about_who_title:"Who Am I?",
about_who_p1:"Hello, I'm Saged, founder of RPL Cyber Security. I'm passionate about cybersecurity, ethical hacking, programming, Linux, Windows and security research.",
about_who_p2:"My goal is to build secure digital solutions and continuously improve my cybersecurity skills.",
about_focus_title:"Current Focus",
about_focus_1:"✔ Security Research",
about_focus_2:"✔ Linux Systems",
about_focus_3:"✔ Ethical Hacking",
about_focus_4:"✔ Python Programming",
about_focus_5:"✔ Network Security",
about_focus_6:"✔ Web Security",

stats_title:"Statistics",
stat_projects_label:"Projects",
stat_hours_label:"Hours Learning",
stat_research_label:"Research",
stat_passion_label:"Passion",

skills_title:"Skills",
skill1_title:"Cyber Security",
skill1_desc:"Security Fundamentals, Blue Team, Research.",
skill2_title:"Linux",
skill2_desc:"Kali Linux, Ubuntu, Bash.",
skill3_title:"Programming",
skill3_desc:"Python, JavaScript, HTML, CSS.",
skill4_title:"Network Security",
skill4_desc:"TCP/IP, Routing, Firewalls, Wireshark.",
skill5_title:"Ethical Hacking",
skill5_desc:"Web Testing, OSINT, Enumeration.",
skill6_title:"Security Tools",
skill6_desc:"Burp Suite, Nmap, Metasploit.",

certificates_title:"Learning Journey",
cert1_desc:"Learning Platform",
cert2_desc:"Labs & Practice",
cert3_desc:"Currently Studying",
cert4_desc:"Future Goal",

journey_title:"Current Mission",
journey_box:
`root@rpl:~$ roadmap

✔ Learn Linux
✔ Learn Networking
✔ Learn Python
✔ Security Research
✔ Build Cyber Tools
✔ Build RPL Company

Status:
██████████░░ 80%

Mission:
Building Secure Digital Solutions.`,

contact_title:"Contact Me",
contact_email_title:"Email",
btn_send_email:"Send Email",
contact_discord_title:"Discord",
contact_discord_desc:"Join Our Community",
btn_join_server:"Join Server",
contact_whatsapp_title:"WhatsApp",
contact_whatsapp_desc:"Contact Directly",
btn_chat_now:"Chat Now",

footer_desc:"Building Secure Digital Solutions.",
footer_home:"Home",
footer_skills:"Skills",
footer_contact:"Contact",
footer_copyright:"© 2026 RPL Cyber Security. All Rights Reserved."

},

ar:{

nav_home:"الرئيسية",
nav_about:"من نحن",
nav_skills:"المهارات",
nav_contact:"تواصل",

loader_text:"جاري تهيئة البيئة الآمنة...",

hero_terminal:"root@rpl:~$ النظام_متصل",
hero_desc:"نبني حلولاً رقمية آمنة من خلال الأمن السيبراني والبرمجة والبحث الأمني والاختراق الأخلاقي.",

btn_skills:"استكشف المهارات",
btn_contact:"تواصل معي",

about_title:"من نحن",
about_who_title:"من أنا؟",
about_who_p1:"مرحباً، أنا Saged، مؤسس RPL Cyber Security. أنا شغوف بالأمن السيبراني والاختراق الأخلاقي والبرمجة ولينكس وويندوز والبحث الأمني.",
about_who_p2:"هدفي هو بناء حلول رقمية آمنة وتطوير مهاراتي في الأمن السيبراني باستمرار.",
about_focus_title:"التركيز الحالي",
about_focus_1:"✔ البحث الأمني",
about_focus_2:"✔ أنظمة لينكس",
about_focus_3:"✔ الاختراق الأخلاقي",
about_focus_4:"✔ برمجة بايثون",
about_focus_5:"✔ أمن الشبكات",
about_focus_6:"✔ أمن الويب",

stats_title:"إحصائيات",
stat_projects_label:"مشاريع",
stat_hours_label:"ساعات تعلم",
stat_research_label:"بحث",
stat_passion_label:"شغف",

skills_title:"المهارات",
skill1_title:"الأمن السيبراني",
skill1_desc:"أساسيات الأمن، الفريق الأزرق، البحث.",
skill2_title:"لينكس",
skill2_desc:"كالي لينكس، أوبونتو، باش.",
skill3_title:"البرمجة",
skill3_desc:"بايثون، جافاسكريبت، HTML، CSS.",
skill4_title:"أمن الشبكات",
skill4_desc:"TCP/IP، التوجيه، الجدران النارية، Wireshark.",
skill5_title:"الاختراق الأخلاقي",
skill5_desc:"اختبار الويب، OSINT، الاستكشاف.",
skill6_title:"أدوات أمنية",
skill6_desc:"Burp Suite، Nmap، Metasploit.",

certificates_title:"رحلة التعلم",
cert1_desc:"منصة تعلم",
cert2_desc:"معامل وتدريب عملي",
cert3_desc:"قيد الدراسة حالياً",
cert4_desc:"هدف مستقبلي",

journey_title:"المهمة الحالية",
journey_box:
`root@rpl:~$ خارطة_الطريق

✔ تعلم لينكس
✔ تعلم الشبكات
✔ تعلم بايثون
✔ البحث الأمني
✔ بناء أدوات أمنية
✔ بناء شركة RPL

الحالة:
██████████░░ 80%

المهمة:
بناء حلول رقمية آمنة.`,

contact_title:"تواصل معي",
contact_email_title:"البريد الإلكتروني",
btn_send_email:"إرسال بريد",
contact_discord_title:"ديسكورد",
contact_discord_desc:"انضم لمجتمعنا",
btn_join_server:"انضم للسيرفر",
contact_whatsapp_title:"واتساب",
contact_whatsapp_desc:"تواصل مباشرة",
btn_chat_now:"ابدأ المحادثة",

footer_desc:"نبني حلولاً رقمية آمنة.",
footer_home:"الرئيسية",
footer_skills:"المهارات",
footer_contact:"تواصل",
footer_copyright:"© 2026 RPL Cyber Security. جميع الحقوق محفوظة."

}

};

/* Words for the hero typing effect, per language */

const wordsByLang = {

en:[
    "Cyber Security Researcher",
    "Ethical Hacker",
    "Python Developer",
    "Linux Enthusiast",
    "Security Researcher"
],

ar:[
    "باحث أمن سيبراني",
    "مخترق أخلاقي",
    "مطور بايثون",
    "عاشق لينكس",
    "باحث أمني"
]

};

/* Live terminal lines, per language */

const terminalLinesByLang = {

en:[

`root@rpl:~$ whoami

Saged

Status : Online ✓`,

`root@rpl:~$ systemctl status firewall

Firewall : Active

Threats : 0`,

`root@rpl:~$ nmap localhost

Scanning...

Ports : 22 80 443

Done.`,

`root@rpl:~$ python scanner.py

Initializing...

Scan Completed

0 Vulnerabilities Found.`,

`root@rpl:~$ echo "Welcome"

Welcome to RPL Cyber Security`

],

ar:[

`root@rpl:~$ whoami

Saged

الحالة : متصل ✓`,

`root@rpl:~$ systemctl status firewall

الجدار الناري : نشط

التهديدات : 0`,

`root@rpl:~$ nmap localhost

جارٍ الفحص...

المنافذ : 22 80 443

تم.`,

`root@rpl:~$ python scanner.py

جارٍ التهيئة...

اكتمل الفحص

0 ثغرات تم اكتشافها.`,

`root@rpl:~$ echo "مرحباً"

مرحباً بكم في RPL للأمن السيبراني`

]

};

/* ==========================
   Language State
========================== */

const langBtn = document.getElementById("langBtn");

let currentLang = localStorage.getItem("language") || "en";

/* These get (re)initialized once the typing + terminal
   sections below are set up, but applyLanguage needs to be
   able to reset them, so declare the hooks here. */

let resetTyping = () => {};
let resetTerminal = () => {};

function applyLanguage(lang){

    document.documentElement.lang = lang;
    document.documentElement.dir = lang === "ar" ? "rtl" : "ltr";

    document.querySelectorAll("[data-lang]").forEach(el => {

        const key = el.dataset.lang;

        if(translations[lang][key]){
            el.textContent = translations[lang][key];
        }

    });

    localStorage.setItem("language", lang);

    resetTyping();
    resetTerminal();

}

langBtn.innerHTML = '<i class="fa-solid fa-language"></i>';

langBtn.onclick = () => {

    currentLang = currentLang === "en" ? "ar" : "en";

    applyLanguage(currentLang);

};

/* ==========================
   Loader
========================== */

window.addEventListener("load", () => {

    const loader = document.querySelector(".loader");

    setTimeout(() => {

        loader.classList.add("hide");

    }, 1800);

});


/* ==========================
   Typing Effect
========================== */

const typingElement = document.getElementById("typing");

let wordIndex = 0;

let charIndex = 0;

let deleting = false;

function typingEffect() {

    const current = wordsByLang[currentLang][wordIndex];

    if (!deleting) {

        typingElement.textContent = current.substring(0, charIndex++);

        if (charIndex > current.length) {

            deleting = true;

            setTimeout(typingEffect, 1500);

            return;

        }

    } else {

        typingElement.textContent = current.substring(0, charIndex--);

        if (charIndex < 0) {

            deleting = false;

            wordIndex++;

            if (wordIndex >= wordsByLang[currentLang].length)

                wordIndex = 0;

        }

    }

    setTimeout(typingEffect, deleting ? 50 : 90);

}

resetTyping = () => {

    wordIndex = 0;
    charIndex = 0;
    deleting = false;

    if(typingElement){
        typingElement.textContent = "";
    }

};

typingEffect();

/* ==========================
   Animated Counters
========================== */

const counters = document.querySelectorAll(".counter");

const counterObserver = new IntersectionObserver((entries) => {

    entries.forEach(entry => {

        if(entry.isIntersecting){

            const counter = entry.target;

            const target = +counter.dataset.target;

            let count = 0;

            const speed = target / 80;

            function updateCounter(){

                count += speed;

                if(count < target){

                    counter.textContent = Math.floor(count);

                    requestAnimationFrame(updateCounter);

                }else{

                    counter.textContent = target;

                }

            }

            updateCounter();

            counterObserver.unobserve(counter);

        }

    });

},{
    threshold:0.5
});

counters.forEach(counter=>{

    counterObserver.observe(counter);

});


/* ==========================
   Fade Animation
========================== */

const animatedItems = document.querySelectorAll(

    ".about-card,.skill-card,.contact-card,.certificate-card,.stat-box,.journey-box"

);

animatedItems.forEach(item=>{

    item.classList.add("fade-up");

});

const observer = new IntersectionObserver((entries)=>{

    entries.forEach(entry=>{

        if(entry.isIntersecting){

            entry.target.classList.add("show");

        }

    });

},{
    threshold:.2
});

animatedItems.forEach(item=>{

    observer.observe(item);

});

/* ==========================
   Back To Top Button
========================== */

const topBtn = document.getElementById("topBtn");

window.addEventListener("scroll", () => {

    if (window.scrollY > 400) {

        topBtn.classList.add("show");

    } else {

        topBtn.classList.remove("show");

    }

});

topBtn.addEventListener("click", () => {

    window.scrollTo({

        top: 0,

        behavior: "smooth"

    });

});


/* ==========================
   Active Navigation
========================== */

const sections = document.querySelectorAll("section");

const navLinks = document.querySelectorAll("nav a");

window.addEventListener("scroll", () => {

    let current = "";

    sections.forEach(section => {

        const sectionTop = section.offsetTop - 120;

        const sectionHeight = section.clientHeight;

        if (scrollY >= sectionTop) {

            current = section.getAttribute("id");

        }

    });

    navLinks.forEach(link => {

        link.classList.remove("active");

        if (link.getAttribute("href") === "#" + current) {

            link.classList.add("active");

        }

    });

});


/* ==========================
   Smooth Scroll
========================== */

navLinks.forEach(link => {

    link.addEventListener("click", function(e) {

        e.preventDefault();

        const target = document.querySelector(this.getAttribute("href"));

        target.scrollIntoView({

            behavior: "smooth"

        });

    });

});

/* ==========================
   Live Terminal
========================== */

const terminal = document.getElementById("terminal");

let terminalIndex = 0;

function updateTerminal(){

    terminal.style.opacity = "0";

    setTimeout(()=>{

        terminal.textContent = terminalLinesByLang[currentLang][terminalIndex];

        terminal.style.opacity = "1";

        terminalIndex++;

        if(terminalIndex >= terminalLinesByLang[currentLang].length){

            terminalIndex = 0;

        }

    },300);

}

resetTerminal = () => {

    terminalIndex = 0;

    if(terminal){
        terminal.style.opacity = "1";
        terminal.textContent = terminalLinesByLang[currentLang][0];
    }

};

setInterval(updateTerminal,4000);


/* ==========================
   Terminal Cursor
========================== */

setInterval(()=>{

    if(!terminal.textContent.endsWith("█")){

        terminal.textContent += " █";

    }else{

        terminal.textContent = terminal.textContent.slice(0,-2);

    }

},600);

/* ==========================
   Mouse Glow Effect
========================== */

const glow = document.createElement("div");

glow.id = "mouseGlow";

document.body.appendChild(glow);

document.addEventListener("mousemove", (e) => {

    glow.style.left = e.clientX + "px";

    glow.style.top = e.clientY + "px";

});


/* ==========================
   Card Tilt Effect
========================== */

const cards = document.querySelectorAll(

".skill-card,.contact-card,.certificate-card,.about-card"

);

cards.forEach(card=>{

    card.addEventListener("mousemove",(e)=>{

        const rect = card.getBoundingClientRect();

        const x = e.clientX - rect.left;

        const y = e.clientY - rect.top;

        const rotateY = (x / rect.width - 0.5) * 12;

        const rotateX = (0.5 - y / rect.height) * 12;

        card.style.transform =

        `perspective(900px)
        rotateX(${rotateX}deg)
        rotateY(${rotateY}deg)
        translateY(-8px)`;

    });

    card.addEventListener("mouseleave",()=>{

        card.style.transform =
        "perspective(900px) rotateX(0) rotateY(0)";

    });

});


/* ==========================
   Navbar Shadow
========================== */

const header = document.querySelector("header");

window.addEventListener("scroll",()=>{

    if(window.scrollY > 80){

        header.style.boxShadow =
        "0 10px 30px rgba(0,0,0,.35)";

    }else{

        header.style.boxShadow = "none";

    }

});


/* ==========================
   Current Year
========================== */

const year = document.getElementById("year");

if(year){

    year.textContent = new Date().getFullYear();

}

/* ==========================
   Safe Element Check
========================== */

function $(selector) {
    return document.querySelector(selector);
}

function $$(selector) {
    return document.querySelectorAll(selector);
}

/* ==========================
   Page Loaded Animation
========================== */

window.addEventListener("load", () => {

    document.body.classList.add("loaded");

});

/* ==========================
   Prevent Double Click
========================== */

document.querySelectorAll("a").forEach(link => {

    link.addEventListener("dragstart", e => {

        e.preventDefault();

    });

});

/* ==========================
   Keyboard Shortcuts
========================== */

document.addEventListener("keydown", (e) => {

    // Ctrl + Home
    if (e.ctrlKey && e.key === "Home") {

        window.scrollTo({

            top: 0,

            behavior: "smooth"

        });

    }

});

/* ==========================
   Performance
========================== */

window.addEventListener("resize", () => {

    document.body.style.overflowX = "hidden";

});

/* ==========================
   Console Message
========================== */

console.clear();

console.log(

"%cRPL Cyber Security",

"color:#00a8ff;font-size:22px;font-weight:bold;"

);

console.log(

"%cWelcome Developer 👋",

"color:#00ff88;font-size:14px;"

);

console.log(

"%chttps://itreppo.github.io/rpl/",

"color:white;font-size:13px;"

);

/* ==========================
   Initial Language Apply
========================== */

applyLanguage(currentLang);

/* ==========================
   End
========================== */

console.log("Website Loaded Successfully.");
